// Versión de la API: súbela cuando cambie el backend. La app compara con la que
// necesita y avisa si el servidor desplegado se quedó atrás (no reiniciado).
const API_VERSION = 44;

module.exports = { API_VERSION };
